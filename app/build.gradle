apply plugin: 'com.android.application'
apply plugin: 'com.neenbedankt.android-apt'
apply from: rootProject.file('basegradle/commonProject.gradle')
apply from: rootProject.file('basegradle/commonAndroid.gradle')

buildscript {
    repositories {
        mavenCentral()
    }

    dependencies {
        classpath 'com.neenbedankt.gradle.plugins:android-apt:1.8'
    }
}

android {
    defaultConfig {
        applicationId rootProject.ext.android_const.packageName
        versionCode 1
        versionName "1.0"
    }

    buildTypes {
        debug {
            minifyEnabled false
            debuggable true
            jniDebuggable true
        }

        release {
            minifyEnabled true
            shrinkResources false

            debuggable false
            jniDebuggable false
            zipAlignEnabled true
            proguardFiles getDefaultProguardFile('proguard-android.txt'), 'proguard-rules.pro'
        }
    }

    configurations {
        all*.exclude group: 'com.google.android', module: 'android'
        all*.exclude group: 'com.google.android', module: 'support-v4'
        all*.exclude group: 'asm', module: 'asm'
        all*.exclude group: 'com.google.code.gson'
    }


    productFlavors {
        normal {

        }

        ddebug {
        }
    }
}

dependencies {
    compile project(':common')
    compile project(':model')
    compile fileTree(dir: 'libs', include: ['*.jar'])
    compile 'com.android.support:appcompat-v7:' + rootProject.ext.android_const.support
    compile 'com.android.support:design:' + rootProject.ext.android_const.support
    compile 'com.squareup.okio:okio:' + rootProject.ext.dep_const.okio
    compile('com.squareup.okhttp:okhttp:' + rootProject.ext.dep_const.okhttp) {
        exclude group: 'com.squareup.okio', module: 'okio'
    }
    //图片加载
    compile 'com.github.bumptech.glide:glide:' + rootProject.ext.dep_const.glide
    //leakcanary
    debugCompile 'com.squareup.leakcanary:leakcanary-android:' + rootProject.ext.dep_const.leakcanary
    releaseCompile 'com.squareup.leakcanary:leakcanary-android-no-op:' + rootProject.ext.dep_const.leakcanary
    //logansquare
    apt 'com.bluelinelabs:logansquare-compiler:' + rootProject.ext.dep_const.logansquare
    compile 'com.bluelinelabs:logansquare:' + rootProject.ext.dep_const.logansquare
    //dagger2 注入
    apt 'com.google.dagger:dagger-compiler:' + rootProject.ext.dep_const.dagger
    compile 'com.google.dagger:dagger:' + rootProject.ext.dep_const.dagger
    //UI注入
    compile 'com.jakewharton:butterknife:' + rootProject.ext.dep_const.butterknife
    //rxjava
    compile 'io.reactivex:rxjava:1.0.10'
    compile 'io.reactivex:rxandroid:0.24.0'
    //网络框架
    compile 'com.squareup.retrofit:retrofit:' + rootProject.ext.dep_const.retrofit
    compile 'com.squareup.retrofit:converter-gson:' + rootProject.ext.dep_const.retrofit
    compile 'com.squareup.retrofit:adapter-rxjava:' + rootProject.ext.dep_const.retrofit

}


